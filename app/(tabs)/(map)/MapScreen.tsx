import { StyleSheet, View } from "react-native";
import Text from "../../../components/typography/Text";

const MapScreen = () => {
  return (
    <View>
      <Text fontSize={40}>Map Screen</Text>
    </View>
  );
};

export default MapScreen;

const MapScreenStyle = StyleSheet.create({
  container: {},
});
